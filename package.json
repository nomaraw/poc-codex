{
  "name": "student-monitoring-pal",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 5173",
    "build": "mkdir -p dist && cp -R index.html src dist/",
    "preview": "python3 -m http.server 4173 -d dist"
  }
}
